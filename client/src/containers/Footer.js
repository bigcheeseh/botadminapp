import React, { Component } from 'react';
import { Layout } from 'antd';

const { Footer } = Layout

export default () => {
    return(
      <Footer style={{ textAlign: 'center' }}>
        Bot admin demo app ©2017
      </Footer>
    )
}
